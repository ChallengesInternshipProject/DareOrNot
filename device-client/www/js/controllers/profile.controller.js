angular.module('starter.controllers')

.controller('ProfileCtrl', function($scope, $http, $log, $localStorage) {
  // $scope.user = $localStorage.user.data;
  //
  // $log.info($localStorage.user);
});
